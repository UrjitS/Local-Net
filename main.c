#include <stdio.h>
#include <stdlib.h>
#include "protocol.h"
// #include "encryption.h"
// #include "routing.h"

int main(int argc, char * argv[]) {
    printf("%lu", sizeof(struct header));

    return EXIT_SUCCESS;
}
